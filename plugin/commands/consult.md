---
description: Search reflections across all agents
argument-hint: "<context query>"
allowed-tools: ["Bash"]
---

Run `legion consult --context '$ARGUMENTS'` and display the results. Consult searches across ALL repos/agents, not just the current one.

Show the results with repo attribution. If any reflections are useful, mention their IDs so the user can boost them.
